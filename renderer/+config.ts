export default {
  meta: {
    title: {
      env: "server-and-client",
      value: "My SSR App"  // ✅ `string` を `value` プロパティに入れる
    },
    description: {
      env: "server-only",
      value: "SSR rendered page for Kamogawa-yu in Kyoto"
    }
  }
};
